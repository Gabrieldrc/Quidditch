export interface RGB {
  r: number,
  g: number,
  b: number,
}

export interface Vector {
  x: number,
  y: number,
}

export interface FieldRange {
  minX: number,
  maxX: number,
  minY: number,
  maxY: number,
}

export interface teamData {
  name: string,
  color: RGB,
}